# Part B Submission Checklist

Use this to verify your `partB/` folder before submission. All items must be satisfied to avoid the 20-mark structure penalty.

## 1. No monolithic notebook in repo root
- [x] **PartB_LatentSVM_Ranking.ipynb** must **not** exist in the repository root. All notebook work must be in `partB/` only. *(Verified: file is deleted.)*

## 2. Eight executed notebooks
- [ ] **task_1_1.ipynb** through **task_3_2.ipynb** — all 8 notebooks present in `partB/`.
- [ ] Every cell has been **run**; outputs and plots are **visible** in the notebooks (missing outputs can attract a 20-mark penalty).
- Run order: execute **task_2_1.ipynb** first (generates data), then the rest.

## 3. Ten LLM disclosure JSONs
- [ ] **llm_task_1_1.json** through **llm_task_4_2.json** — all 10 files present in `partB/`.
- [ ] Each JSON includes the **mandatory** Part B fields: **task_tag**, **code_used_verbatim**, **student_modification**.

## 4. Report
- [ ] **report.pdf** in `partB/` — maximum 4 pages, synthesizing paper summary, reproduction results, ablations, failure modes, and reflection. *(report_draft.md is a placeholder; export your final report as report.pdf.)*

## 5. requirements.txt
- [ ] **partB/requirements.txt** — lists all CPU-installable dependencies with **exact version numbers**.

## 6. results/ folder
- [ ] **partB/results/** — contains all plots and visualizations (e.g. task_2_3_results.png, task_3_1_ablation.png, task_3_2_failure_mode.png, results_df.csv).

## 7. data/ folder
- [ ] **partB/data/** — contains the **toy** dataset files: **train_data.npy**, **test_data.npy**, **scaler.npy** (generated by task_2_1.ipynb).
- [ ] **partB/data/README.md** — explains how the dataset was **obtained** and **used**. No use of the paper’s original dataset (e.g. LETOR OHSUMED) for Part B.

---

*Last verified: Part B uses only synthetic data from partB/task_2_1.ipynb; root README no longer references PartB_LatentSVM_Ranking.ipynb or training on LETOR OHSUMED.*
